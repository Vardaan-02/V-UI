import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com","api.microlink.io",], // Add the Unsplash domain here
  },
};

export default nextConfig;
